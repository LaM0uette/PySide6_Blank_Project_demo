class Combo_box:
    def __init__(self,
                 colors_type,
                 colors,
                 dim,
                 img
    ):

        self.colors_type = colors_type
        self.colors = colors
        self.dim = dim
        self.img = img

        print(self.colors_type)
        print(self.colors)
        print(self.dim)
        print(self.img)
