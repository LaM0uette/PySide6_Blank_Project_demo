from PySide6.QtCore import Qt


class TextFormat:

    def plain_text(self): return Qt.PlainText
    def rich_text(self): return Qt.RichText
    def auto_text(self): return Qt.AutoText
    def plain_text(self): return Qt.PlainText
